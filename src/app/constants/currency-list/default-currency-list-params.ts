import { ICurrencyListParams } from '../../core';

export const defaultCurrencyListParams: ICurrencyListParams = {
    start: 1,
    limit: 10,
    sort: 'marketCap',
    sortDirection: 'descend',
};
